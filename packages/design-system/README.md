# Design System

Identidade visual compartilhada pelos módulos do BASE.

## Componentes atuais

- `AppSwitcher`: marca BASE e seletor para alternar entre aplicativos.
- `styles.css`: estilos do seletor e escala tipográfica comum.

O pacote contém apenas elementos que já possuem mais de um consumidor real.

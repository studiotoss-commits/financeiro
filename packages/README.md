# Pacotes compartilhados

Os diretórios reservam `design-system`, `autenticacao`, `banco-de-dados`, `notificacoes` e `configuracoes`.

Eles ainda não possuem `package.json` nem código. Uma capacidade só se torna pacote quando houver reutilização real, contrato público explícito e consumidores independentes.
